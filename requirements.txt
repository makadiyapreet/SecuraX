# =============================================================
# Automated Code Vulnerability Detection — Requirements
# All packages are free/open-source (MIT/Apache-2.0/BSD/GPL)
# =============================================================

# ---------- Deep Learning & NLP ----------
torch>=2.1.0
transformers>=4.46.0,<5.0.0
tokenizers>=0.20.0,<0.22.0
peft>=0.7.0
bitsandbytes>=0.41.0        # 4-bit quantization for LLMs (CUDA)
accelerate>=0.25.0           # model loading & device mapping
datasets>=2.16.0             # HuggingFace dataset loading
sentencepiece>=0.1.99        # tokenizer backend for some models
protobuf>=3.20.0             # required by sentencepiece

# ---------- Data Science & Metrics ----------
scikit-learn>=1.3.0
pandas>=2.1.0
numpy>=1.24.0

# ---------- Visualization ----------
matplotlib>=3.8.0
seaborn>=0.13.0

# ---------- Jupyter ----------
jupyter>=1.0.0
ipykernel>=6.27.0

# ---------- Static Analysis ----------
bandit>=1.7.6                # Python vulnerability scanner
# NOTE: cppcheck is a system-level tool — install via:
#   macOS:  brew install cppcheck
#   Ubuntu: sudo apt-get install cppcheck

# ---------- Web App ----------
streamlit>=1.29.0

# ---------- Optional: vLLM (faster local LLM serving) ----------
# Uncomment if you want to use vLLM instead of HF Transformers
# for inference with Mistral/DeepSeek:
# vllm>=0.2.7
